export default async function handler(req, res) {
  try {
    const { address } = req.body;

    const price = 650000000; // 테스트값

    res.status(200).json({
      officialPrice: price,
      source: "공시가격 API",
      transactionCount: 1
    });

  } catch (e) {
    res.status(500).json({
      error: "API 실패"
    });
  }
}